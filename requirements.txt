# Runtime dependencies for the ACIS risk-analytics pipeline.
# Versions are constrained: shap cannot parse boosters produced by xgboost 3.x
# (ValueError: could not convert string to float: '[...]'), which breaks the
# Task 4 SHAP interpretation step.
pandas>=2.0,<3.0
numpy>=1.26,<3.0
matplotlib>=3.7,<4.0
seaborn>=0.13,<0.14
scipy>=1.11,<2.0
statsmodels>=0.14,<0.15
scikit-learn>=1.3,<2.0
joblib>=1.3,<2.0
xgboost>=2.0,<3.0
shap>=0.44,<0.50
dvc>=3.0,<4.0
jupyter>=1.0,<2.0
